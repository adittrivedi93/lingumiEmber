<div class="page_content">
	<div id="the_apps" class="row well_padded">
		<h2>{{_ "<strong>The Apps:</strong>" }}</h2>
		<h4>{{_ "Our first app is available now with the Learning Cubes: we'll send you a link to download it (it's not available on the app store to just anyone!). First Verbs, our second app which continues your child's learning progress, is being built at the moment, and will also be free." }}</h4>
		<div class="col-md-6">
			<img src="assets/first_words_icon.png">
			<h2>{{_ "Lingumi's First Word" }}</h2>
			<h4>{{_ "<strong>Play and learn:</strong>" }}</h4>
				<p>{{_ "Sounds from native speakers" }}</p>
				<p>{{_ "60+ nouns, selected from the <strong>Oxford CDI</strong>" }}</p>
				<p>{{_ "Answer questions & solve puzzles" }}</p>
				<p>{{_ "Learn contextual language" }}</p>
				<p>{{_ "Start using first phrases in a new language" }}</p>
		</div>
		<div class="col-md-6">
			<img src="assets/first_verbs_icon.png">
			<h2>{{_ "Lingumi's First Verbs" }}</h2>
			<h4><strong>{{_ "Play and learn:</strong>" }}</h4>
				<p>{{_ "20+ verbs pronounced by native speakers" }}</p>
				<p>{{_ "Multiple verb forms" }}</p>
				<p>{{_ "Contextual usage & implicit grammar" }}</p>
				<p>{{_ "Adapting grammatical forms" }}</p>
				<p>{{_ "50+ adjectives, prepositions, and more" }}</p>
		</div>
	</div>
</div>